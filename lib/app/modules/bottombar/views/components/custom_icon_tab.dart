import 'package:flutter/material.dart';

class CustomIconTab extends StatelessWidget {
  const CustomIconTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}